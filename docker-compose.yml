services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: zinc
      POSTGRES_USER: zinc
      POSTGRES_PASSWORD: zinc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
